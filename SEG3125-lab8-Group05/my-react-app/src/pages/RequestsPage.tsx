import { PageTemplate } from './PageTemplate'

export function RequestsPage() {
  // TEAM TODO (Requests): Build request feed with status, tags, and submit-request actions.
  return <PageTemplate name="Requests" />
}
