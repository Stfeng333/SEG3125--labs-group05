import { PageTemplate } from './PageTemplate'

export function RequestDetailsPage() {
  // TEAM TODO (Request Details): Build request thread with replies and discussion state.
  return <PageTemplate name="Request Details" />
}
