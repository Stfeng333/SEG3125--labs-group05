import { PageTemplate } from './PageTemplate'

export function CoursesPage() {
  // TEAM TODO (Courses): Build course directory with search/filter and enrollment state.
  return <PageTemplate name="Courses" />
}
