import { PageTemplate } from './PageTemplate'

export function NotesPage() {
  // TEAM TODO (Notes): Build notes listing cards, tags, filters, and sorting controls.
  return <PageTemplate name="Notes" />
}
