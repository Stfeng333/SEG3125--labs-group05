import { PageTemplate } from './PageTemplate'

export function HomePage() {
  // TEAM TODO (Home): Build dashboard sections (search, note feed, filters, recommendations).
  return <PageTemplate name="Home" />
}
