import { PageTemplate } from './PageTemplate'

export function ClassesPage() {
  // TEAM TODO (Classes): Build class hub cards and navigation to related courses/notes.
  return <PageTemplate name="Classes" />
}
